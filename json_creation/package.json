{
  "name": "json_creation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon --exec \"json-server db.json --watch db.json --port 3000\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.9.0",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
