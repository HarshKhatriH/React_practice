const heading = React.createElement("h1",
    {
        
    },"Hii How are you");
    // console.log(heading);
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);
// console.log(root);
